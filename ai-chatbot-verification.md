# AI Prediction chatbot verification

The desktop screenshot at 1280×720 confirms the new AI Prediction view opens as the primary workspace. It visibly includes the decision-copilot hero, confidence score, project-aware chat header, conversation welcome message, evidence cards, question input area below the conversation, and quick-question rail.

The mobile screenshot at 390×844 confirms the hero and chat header stack cleanly within the responsive layout. The hamburger navigation remains available, the Shift B context remains visible, and the chat panel does not overflow horizontally. The conversation and input continue below the viewport as expected for a scrollable module.

The chatbot is intentionally implemented as a deterministic frontend project knowledge layer. It answers questions from the selected shift's orders, inventory, picking, dispatch, and analytics data; it does not claim to connect to a real LLM, backend, private data, or authentication service.
