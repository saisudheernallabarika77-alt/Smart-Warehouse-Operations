# Account personalization verification

The sidebar account control now displays **Sudheer** with the initials **SD** and the role **Fulfillment lead**. On desktop, the control is anchored at the bottom of the persistent operations rail and remains visible at the standard 1280×720 viewport. On mobile, the same account control is available inside the responsive operations menu.

The account control opens a right-side account panel with the following working details: Sudheer, Fulfillment Lead, Northline FC-01 in Manchester, the currently selected shift and time window, operations control-room access, active demo session state, frontend demo authentication mode, and a clear notice that real credentials are not stored because this is a frontend-only hackathon build. The panel can be closed by the close icon, the backdrop, or the Close account details button.

Desktop and mobile dashboard screenshots were captured after the change. The final desktop screenshot confirms the Sudheer account entry is visible in the sidebar. TypeScript checking and the production build both pass.
